version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - echo "No Maven build — just using source files"

  post_build:
    commands:
      - echo "Build complete"

artifacts:
  files:

    - target/*.*

